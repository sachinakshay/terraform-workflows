name: Terraform CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Clone terraform-modules repository
      - name: Clone terraform-modules repository
        run: git clone https://github.com/sachinakshay/terraform-modules.git terraform-modules

      # Step 3: Debugging - List the directory structure
      - name: List directory structure
        run: |
          echo "List files in the working directory:"
          ls -R /home/runner/work/terraform-workflows/terraform-workflows/

      # Step 4: Set up Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      # Step 5: Initialize Terraform for EC2 Module
      - name: Terraform Init (EC2 Module)
        working-directory: terraform-modules/modules/ec2  # Updated path to ec2 module
        run: terraform init

      # Step 6: Terraform Plan for EC2 Module
      - name: Terraform Plan (EC2 Module)
        working-directory: terraform-modules/modules/ec2  # Updated path to ec2 module
        run: terraform plan

      # Step 7: Terraform Apply (Only on Push to Main Branch)
      - name: Terraform Apply (EC2 Module)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: terraform-modules/modules/ec2  # Updated path to ec2 module
        run: terraform apply -auto-approve
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      # Repeat similar steps for s3 and vpc modules if needed
